PREFIX skos: <http://www.w3.org/2004/02/skos/core#>

# all properties of Choggy Butte Mountain
SELECT distinct ?predicate ?object WHERE {
	?uri skos:prefLabel "Choggy Butte Mountain" ;
  	 	 ?predicate ?object .
}
